# Used by Cursor MCP for GitHub (see docs/github-mcp-cursor.md).
# Copy to .env and load into your environment, or export in your shell before starting Cursor.
# Do not commit real tokens.
GITHUB_MCP_PAT=
