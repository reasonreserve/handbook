# Used by Cursor MCP for GitHub (see docs/github-mcp-cursor.md).
# Copy to .env — do not commit real tokens.
#
# Automation: ./scripts/open-cursor.sh loads this .env then starts Cursor.
# Or: brew install direnv, hook zsh, run `direnv allow`, then `cursor .` from this repo.
GITHUB_MCP_PAT=
