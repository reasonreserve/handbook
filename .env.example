# Optional: only if you use ${env:GITHUB_MCP_PAT} in ~/.cursor/mcp.json
# or ./scripts/open-cursor.sh / direnv — not required if the PAT is pasted
# directly into ~/.cursor/mcp.json (Spotlight-friendly). See docs/github-mcp-cursor.md
GITHUB_MCP_PAT=
