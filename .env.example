# Copy to `.env` only when this project needs local env vars (e.g. future app code).
# GitHub MCP does NOT read this file — put your GitHub PAT in `.cursor/mcp.json` only.
# See docs/github-mcp-cursor.md
