# direnv: run `brew install direnv` and add the hook to ~/.zshrc, then `direnv allow` once.
# After that, any terminal session with cwd under this repo gets GITHUB_MCP_PAT from .env.
# Launch Cursor with: cursor .  (from this directory) so the app inherits the same environment.
if [[ -f .env ]]; then
  dotenv
fi
